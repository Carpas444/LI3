


#ifndef VALIDACAOSINTMUSICS_H
#define VALIDACAOSINTMUSICS_H

/// @brief  Verifica se uma música é sintaticamente válida
/// @param  music A música que se pretende verificar se é válida 
/// @return Um int que serve apenas como valor booleano
int validaSint_music(char** music);

/// @brief  Verifica se um determinado ano é válido
/// @param  year O ano que se pretende verificar se é válido. É convertido de string para int para se realizar a validação 
/// @return Um int que serve aenas como valor booleano
int valida_ano(char* year);

#endif